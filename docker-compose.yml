version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - INPUT_DIM=100000
      - CONS_SOROBAN_RPC=https://soroban-testnet.stellar.org
      - CONS_CONTRACT_ID=REPLACE_WITH_CONTRACT_ID
      - CONS_SIGNER_SECRET=
    volumes:
      - ./:/app
